{
  "packages": ["packages/*"],
  "npmClient": "yarn",
  "version": "independent",
  "workspaces": true,
  "command": {
    "version": {
      "message": "chore(release): release",
    },
    "publish": {
      "ignoreChanges": ["ignored-file", "*.md"],
      "conventionalCommits": true
    }
  }
}
